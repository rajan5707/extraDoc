curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.34.0/install.sh | bash
. ~/.nvm/nvm.sh
nvm install --lts
node -e "console.log('Running Node.js ' + process.version)" #nodejs version
###########################
ANGULAR INSTALL
sudo apt install -y build-essential
npm install -g @angular/cli
